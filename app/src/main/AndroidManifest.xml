<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.health_tracker_v2">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Update"></activity>
        <activity android:name=".TakeDaily" />
        <activity
            android:name=".PHT"
            android:label="@string/title_activity_pht"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".EER"
            android:label="@string/title_activity_eer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Sign_up2" />
        <activity android:name=".Sign_in" />
        <activity
            android:name=".BMI"
            android:label="@string/title_activity_bmi"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".User_Profile"
            android:label="@string/title_activity_user__profile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>